import { Card, CardContent, CardFooter, CardTitle } from "@/components/ui/card";

const ExerciseCard = () => {
  return (
    <Card>
      <CardTitle>Exercise</CardTitle>
      <CardContent>Exercise card content</CardContent>
      <CardFooter>Footer</CardFooter>
    </Card>
  );
};

export { ExerciseCard };
